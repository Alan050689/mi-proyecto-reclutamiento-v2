export default function BusquedaPage({ params }) {
  const { busquedaId } = params;

  // Acá va tu lógica/render real. Ejemplo mínimo:
  return (
    <div>
      <h1>Búsqueda: {busquedaId}</h1>
      {/* Agregá tu código real abajo */}
    </div>
  );
}
